# instructions

1. get `build-essential libgl1-mesa-dev xorg-dev libglfw3-dev`, install as packages?
2. get `glm` and `glad` as include, place in `/include`
3. `cmake .`
4. `make`
5. `./bin/ATELIE`

current camera move: wasdqe, mouse to look around
change mesh: 1, 2